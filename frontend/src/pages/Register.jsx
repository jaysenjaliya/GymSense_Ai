// Registration form: name, email, password, age, gender, height, weight, goal.
export default function Register() {
  // TODO: form -> api/auth.register, then auto-login
  return <div className="p-8">Register</div>;
}
