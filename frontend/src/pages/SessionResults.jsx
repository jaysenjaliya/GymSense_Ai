// Single-session results: duration, exercises, sets/reps, calories, summary,
// plus "Analyze My Workout" -> POST /llm/analyze for AI recommendations.
export default function SessionResults() {
  // TODO: fetch session by :id, render charts + analyze button
  return <div className="p-8">Session Results</div>;
}
