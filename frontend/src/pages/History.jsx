// Workout history: list past sessions with date + summary, link to results.
export default function History() {
  // TODO: fetch list via api/sessions.listSessions, render table/cards
  return <div className="p-8">History</div>;
}
