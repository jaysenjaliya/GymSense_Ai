// Login form -> POST /auth/login, store tokens, redirect to dashboard.
export default function Login() {
  // TODO: form (email, password), call api/auth.login, handle errors
  return <div className="p-8">Login</div>;
}
