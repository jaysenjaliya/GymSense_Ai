"""Persistence model for the `users` collection (profile + auth fields)."""
# TODO: define the stored user document shape (hashed_password, profile fields,
#       created_at). Maps to MongoDB `users` collection.
