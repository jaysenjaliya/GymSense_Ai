"""Persistence models for `sessions` and `exercise_logs` collections.

`sessions`       : raw upload metadata + computed analytics results.
`exercise_logs`  : per-exercise breakdown within a session.
"""
# TODO: define stored document shapes for both collections.
