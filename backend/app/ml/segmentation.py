"""Exercise segmentation — group the smoothed prediction sequence into contiguous
exercise segments (label + start/end window indices)."""


def segment(predictions: list[int]):
    """Return contiguous segments as (label, start_idx, end_idx) tuples."""
    # TODO
    ...
