"""Analytics routes: aggregate stats across a user's session history."""
from fastapi import APIRouter

router = APIRouter()

# TODO: GET /summary  -> aggregate metrics (volume, frequency, trends) for dashboard
